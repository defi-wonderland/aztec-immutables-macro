name: Update Benchmark Baseline

on:
  push:
    branches: [dev, main]
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch:

jobs:
  baseline:
    uses: defi-wonderland/aztec-ci-actions/.github/workflows/update-baseline.yml@dev
    secrets: inherit
